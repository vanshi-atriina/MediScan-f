import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'MediScan - Medical Document Text Extraction',
  description: 'Professional medical document OCR and text extraction service using AI',
  keywords: 'medical, documents, OCR, text extraction, healthcare, AI, Gemini',
  authors: [{ name: 'MediScan Team' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}